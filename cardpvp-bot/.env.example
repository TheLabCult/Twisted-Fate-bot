# Get this from https://discord.com/developers/applications -> your app -> Bot -> Token
DISCORD_TOKEN=

# Optional: your test server's ID, for instant slash-command sync during development.
# Right-click your server icon in Discord (with Developer Mode on) -> Copy Server ID.
# Leave blank for global sync (works everywhere, but can take up to an hour to propagate).
DEV_GUILD_ID=
